# Python Text Analyzer

A small Python project to analyze text files for word frequency, character count, and sentence statistics.

## Features
- Count words, sentences, and characters
- Find most common words
- Support multiple text files
- Easy-to-use command-line interface

## Folder Structure
- `src/` → Python source code
- `tests/` → Unit tests
- `data/` → Sample text files
- `docs/` → Documentation
- `.venv/` → Virtual environment

# Documentation - Python Text Analyzer

## How to Run
1. Activate virtual environment:

2. Install dependencies:

3. Run main program:


## Testing


## Project Structure
- `src/` → Python code  
- `tests/` → Unit tests  
- `data/` → Sample text files  
- `.venv/` → Virtual environment

Start Coding

Create .venv:

python -m venv .venv


Activate:

.venv\Scripts\activate


Install dependencies:

pip install -r requirements.txt


Run project:

python src/main.py


Run tests:

python -m unittest tests/test_analyzer.py


Initialize Git and push:

git init
git add .
git commit -m "Initial commit: Python Text Analyzer"
git remote add origin git@github.com:farooqyousaf2025/python-text-analyzer.git
git push -u origin master"# python-text-analyzer" 
